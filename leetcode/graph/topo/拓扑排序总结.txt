拓扑排序可以用bfs 也可以用 dfs
每次我们先建立图， 然后找到入度为0 的节点
再把所有入读为0的节点当成起点
把起点先加入 result中，再把起点给去掉，
起点去掉之后 也需要把 起点的邻居 的入度 也减去 因为每次去掉一个节点 对应的边也被去掉了

拓扑排序 可以判断一个图中有没有环

判断一个图中有没有环来说， 其实用dfs更快
因为dfs 可以在使用过程中直接判断有没有环
但是  bfs 不行，bfs必须在结束后判断 所有节点是不是被访问过了