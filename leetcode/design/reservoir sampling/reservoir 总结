





reservoir  sampling 是从 一堆不知道长度的数据中，想随机抽取 k 个数据，每个需要等概率

思路是 创建长度为k 的array， k = 1时 用一个变量替代
然后， 有一个count 来计算当前遇到了几个数据，初始化为 0

k = 1 时：
    每次 random 从0 到count 抽取数字，如果随机的数字等于 count，那么更新 结果， 这样一直到数据结束，都是等概率的

k 大于 1时：
    如果小于 count 小于k，直接放进结果array， 大于k 时， random 从 0 到count， 如果随机的数字小于等于k的话， 把数组中 index 随机的数 更新乘当前的